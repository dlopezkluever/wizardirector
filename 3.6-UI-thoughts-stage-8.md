Current Layout (top to bottom)

The sidebar (SceneAssetListPanel.tsx) is structured as:

1. Header (lines 436-547) — Title, stats, then 4 stacked full-width buttons:
- Inherit from prior scene
- Add from existing assets
- Create new asset
- Detect with AI
2. Search & Filters (lines 549-653) — Search input + Filters dropdown
3. Scrollable asset list (lines 655-723) — Already wrapped in <ScrollArea
className="flex-1">, so it should scroll
4. Bottom bar (lines 725-778) — Generate Visuals, conditional Use Master As-Is, then 
Back / Proceed

Problem Diagnosis

The ScrollArea is already there (line 655), so technically the asset list is
scrollable. The real issue is that the non-scrollable regions above and below eat    
~400px of vertical space, leaving barely any room for the scroll viewport. The 4     
stacked full-width buttons alone consume ~150px. The bottom bar with Generate Visuals   + Back/Proceed is another ~120px.

Recommendations

Option A: Compact button toolbar (your suggestion — I recommend this)

┌────────────┬───────────────────────────────────────────────────────────────────┐   
│    Area    │                              Change                               │   
├────────────┼───────────────────────────────────────────────────────────────────┤   
│ Top        │ 2×2 grid: Row 1 = Add from existing assets (full width or with    │   
│ buttons    │ Create new), Row 2 = Inherit from prior scene | Detect with AI.   │   
│            │ Use icon-only or short labels.                                    │   
├────────────┼───────────────────────────────────────────────────────────────────┤   
│ Generate   │ Move up into the Filters row (line 570), placed inline to the     │   
│ Visuals    │ right of the Filters dropdown. It's always visible and doesn't    │   
│            │ waste a bottom footer slot.                                       │   
├────────────┼───────────────────────────────────────────────────────────────────┤   
│            │ Simplified to just Back / Proceed — much slimmer. The conditional │   
│ Bottom bar │  Use Master As-Is could become a secondary action in the Filters  │   
│            │ row or a dropdown option.                                         │   
└────────────┴───────────────────────────────────────────────────────────────────┘   

This saves ~150-180px of vertical space — roughly 2 more asset cards visible.        

Option B: Collapsible toolbar

Keep all buttons but wrap them in a collapsible/accordion section (e.g., "Actions    
▾"). Collapsed by default after first use. Less invasive but hides functionality.    

Option C: Overflow menu

Move Inherit, Create new, and Detect with AI into a single + dropdown menu. Only Add 
from existing assets stays visible. Most compact but least discoverable.

---
My Recommendation: Option A with these specifics

1. Button layout — 2-row compact grid:
- Row 1: + Add from existing assets (full width, outline)
- Row 2: two half-width buttons side by side — ↻ Inherit | 🧠 Detect with AI       
- + Create new asset becomes a text link or is folded into the Add button's        
dropdown
2. Generate Visuals — Move into the filter/search bar area, right-aligned next to    
Filters. Show the count badge. When nothing is selected, it's disabled/dimmed but    
still visible so users know the workflow.
3. Use Master As-Is — Move to a secondary action. Either:
- A small icon button next to Generate Visuals, or
- An option inside a dropdown menu attached to Generate Visuals (split button      
pattern)
4. Bottom bar — Only ← Back and ✓ Proceed, no divider/spacer bloat.
5. Optional polish: The Create new asset inline form (lines 502-546) currently     
expands inside the header section, pushing everything down. It could instead open as 
a small dialog/popover so it doesn't displace the asset list.

---
Want me to proceed with Option A, or would you like to tweak any of these choices    
first?


## **FULL PLAN:**

╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌ Stage 8: Sidebar Layout Compaction & Card Clipping Fix

 Context

 The Stage 8 sidebar (SceneAssetListPanel.tsx) has two problems:
 1. Cards are clipped — the left border highlight and right status badges are cut off  
 because the ScrollArea's inner <div className="p-2"> gives only 8px padding while     
 cards have borders + the whileHover={{ scale: 1.01 }} causes overflow.
 2. Too little scroll space — 4 stacked full-width action buttons + bottom bar with    
 Generate Visuals + Use Master As-Is + Back/Proceed consume ~400px of vertical space,  
 leaving minimal room for the scrollable asset list.

 File

 src/components/pipeline/Stage8/SceneAssetListPanel.tsx

 ---
 Change 1: Fix card clipping in ScrollArea

 Lines 655–656 — The <div className="p-2"> inside ScrollArea is too tight.

 - Change p-2 → px-3 py-2 so cards have enough horizontal breathing room for their     
 border + hover scale effect.

 ---
 Change 2: Compact action buttons into 2-row grid

 Lines 446–500 — Currently 3 stacked full-width buttons (Inherit, Add existing, Create 
  new) + Detect with AI.

 Replace with a compact layout:
 - Row 1: + Add from existing assets (full width, outline) — this is the primary add   
 action
 - Row 2: Two half-width buttons side by side: ↻ Inherit | 🧠 Detect with AI
 - + Create new asset moves to a ghost text button below row 2, or a smaller compact   
 row

 Specifics:
 <div className="flex flex-col gap-1.5 mt-2">   ← keep this wrapper
   Row 1: Add from existing assets (full width, outline, size="sm")  ← stays as-is     
   <div className="flex gap-1.5">                ← NEW: side-by-side row
     Inherit from prior scene (flex-1, ghost, size="sm", shorter label: "Inherit")     
     Detect with AI (flex-1, gold, size="sm")
   </div>
   Create new asset (ghost, size="sm", full width) ← stays as-is
 </div>

 Key changes:
 - Inherit from prior scene button moves from the header area (lines 446–463) DOWN     
 into the action buttons grid, and its label shortens to just Inherit (icon provides   
 context).
 - The side-by-side row uses flex gap-1.5 with both buttons flex-1.

 ---
 Change 3: Move "Generate Visuals" into the Filters row

 Lines 570–627 (Filters row) and Lines 726–745 (Generate Visuals button in bottom      
 bar).

 Move the Generate Visuals button to sit inline with the Filters dropdown:

 <div className="flex items-center gap-2">
   [Filters dropdown]                              ← existing
   <div className="flex-1" />                      ← spacer pushes Generate right      
   [Generate Visuals button - compact, size="sm"]  ← moved here from bottom
 </div>

 The Generate Visuals button becomes size="sm" with shorter text when no items
 selected: just the sparkles icon disabled. When items are selected: ✦ Generate (N).   

 ---
 Change 4: Search bar "Clear" behavior

 Lines 549–627 — When any filter is active (search text, tag filters, etc.), show a    
 "Clear" button INLINE with the search bar (to its right), NOT below it.

 Replace the current layout:
 [Search input .........................]     ← full width
 [Filters]  [Clear all]                      ← below

 With:
 [Search input .................] [Clear]     ← Clear inline, squishes search
 [Filters] [Generate Visuals (N)]            ← Filters + Generate on same row

 Implementation:
 - Wrap the search <div className="relative"> and the Clear button in a <div
 className="flex items-center gap-2">.
 - The Clear button is a small ghost button labeled "Clear" (not "Clear all").
 - It only appears when any filter is active: filters.searchQuery ||
 filters.tagFilters.length > 0 || filters.hasTagsOnly || filters.carryForwardOnly.     
 - Remove the old "Clear all" button from the Filters row (line 622–626).

 ---
 Change 5: Slim down the bottom bar

 Lines 725–778 — Currently holds: Generate Visuals, Use Master As-Is (conditional),    
 divider, Back/Proceed.

 After moving Generate Visuals up (Change 3), the bottom bar becomes:

 <div className="p-3 border-t border-border/50 space-y-2">    ← p-4 → p-3
   [Use Master As-Is button]     ← only shows when selection > 0, stays here
   <div className="flex gap-2">
     [← Back]  [✓ Proceed]
   </div>
 </div>

 - Remove the Generate Visuals button from here (it's now in the filters row).
 - Remove the extra <div className="border-t border-border/50 pt-2" /> divider — the   
 border-t on the wrapper is sufficient.
 - Reduce padding from p-4 to p-3.

 ---
 Summary of vertical space savings

 ┌───────────┬──────────────────────┬────────────────────────┬──────────────────┐      
 │   Area    │        Before        │         After          │      Saved       │      
 ├───────────┼──────────────────────┼────────────────────────┼──────────────────┤      
 │ Action    │                      │ ~85px (1 full + 1      │                  │      
 │ buttons   │ ~150px (4 stacked)   │ side-by-side + 1       │ ~65px            │      
 │           │                      │ compact)               │                  │      
 ├───────────┼──────────────────────┼────────────────────────┼──────────────────┤      
 │ Bottom    │ ~130px (Generate +   │ ~75px (Master          │                  │      
 │ bar       │ Master + divider +   │ conditional + nav)     │ ~55px            │
 │           │ nav)                 │                        │                  │      
 ├───────────┼──────────────────────┼────────────────────────┼──────────────────┤      
 │           │                      │                        │ ~120px (~1.5     │      
 │ Total     │                      │                        │ extra cards      │      
 │           │                      │                        │ visible)         │      
 └───────────┴──────────────────────┴────────────────────────┴──────────────────┘      

 ---
 Verification

 1. Open Stage 8 with a scene that has 5+ assets
 2. Confirm asset cards are NOT clipped — left border highlight and right status       
 badges fully visible
 3. Confirm the scrollable area shows more cards than before
 4. Confirm "Add from existing", "Inherit" | "Detect with AI", and "Create new"        
 buttons all work
 5. Confirm "Generate Visuals (N)" appears in the filters row and works correctly      
 6. Type in search → "Clear" button appears inline to the right of the search bar →    
 clicking it clears all filters
 7. Select assets with checkboxes → "Use Master As-Is" still appears in bottom bar     
 8. Back / Proceed buttons still work
 9. npm run lint from project root
 10. npm run lint from backend dir