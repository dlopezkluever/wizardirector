● Here's a summary of everything implemented for Phases G & H:

---

Compact summary
  ⎿  This session is being continued from a previous conversation that ran out of   
     context. The summary below covers the earlier portion of the conversation.     

     Summary:
     1. Primary Request and Intent:
        The user asked me to: (1) Read the full master specification at
     `._docs/3.7-location-system-enhancement.md` before doing anything. (2)
     Understand the full architecture — 5 layers, data model, and implementation    
     phases A through H. (3) Read the compact summaries of phases A-F that were     
     already implemented. (4) Implement Phases G (Established Views) and H (Batch   
     Generation) of the location system enhancement.

        Phase G scope: Lock-as-reference button in Stage 10 + gap detection + feed  
     back into views — progressive consistency that grows with production. When a   
     generated frame is approved in Stage 10, the system checks if it fills a       
     coverage gap (direction with no image). If yes, proactively suggests locking   
     the frame as an "established view." If the direction already has an image,     
     offers as secondary action with replace dialog.

        Phase H scope: Auto-propose + batch generate from coverage gaps —
     convenience feature. "Generate Missing Views" button in Stage 8 coverage panel 
     that identifies directions without images and generates them using the
     establishing/primary image as a STYLE reference.

     2. Key Technical Concepts:
        - **5-layer location system**: Sub-location Precision, Camera Directions    
     (multi-view refs), Per-Shot Direction Assignment, Coverage Analysis,
     Established Views (hero frames)
        - **`location_views` table**: FK to `project_assets`, supports
     `establishing` + `direction` view types, `is_primary` fallback semantics,      
     `source` tracking (user/established/stage7_inferred)
        - **Established Views (hero frame pattern)**: Industry-standard technique — 
     lock approved generated frames as reference images for future shots from the   
     same direction
        - **ImageGenerationService job types**: `master_asset` auto-updates
     `project_assets.image_key_url`; `scene_asset` requires sceneId+assetId to      
     update `scene_asset_instances`; using `scene_asset` WITHOUT assetId/sceneId    
     avoids any auto-updates
        - **Style reference images**: Location views use `REFERENCE_TYPE_STYLE`     
     (role: 'style') not identity — captures visual tone, color palette, atmosphere 
        - **ShotWithFrames**: Frame data type used in Stage 10, now extended with   
     `cameraDirectionId` for direction lookup
        - **React hooks rules**: Hooks must be called before any conditional returns      (fixed lint error)

     3. Files and Code Sections:

        - **`._docs/3.7-location-system-enhancement.md`** (READ)
          - Master specification for the entire location enhancement system (5      
     layers, 8 phases A-H, ~715 lines)
          - Defines LocationView interface, location_views table schema, coverage   
     analysis, established views, prompt generation changes

        - **`._docs/3.7-location-session-compact-A&B.md`** (READ)
          - Summary of Phase A (migration 038, LocationView type, Shot camera       
     fields) and Phase B (compound heading parsing, sub-location precision)

        - **`._docs/3.7-location-session-compact-C&D.md`** (READ)
          - Summary of Phase C (location view CRUD, LocationViewsDialog.tsx) and    
     Phase D (parseCameraMetadata, ShotExtractionResult, stage7_inferred directions)
        - **`._docs/3.7-location-session-compact-E&F.md`** (READ)
          - Summary of Phase E (LocationCoveragePanel, shot update whitelist,       
     shotService camera fields) and Phase F (matchShotToLocationView,
     buildLocationDeltaDescription, enrichAssetsWithAngleMatch,
     buildNumberedImageManifest, REFERENCE_TYPE_STYLE)

        - **`backend/src/services/frameGenerationService.ts`** (MODIFIED)
          - Added `camera_direction_id` to the SELECT query in
     `fetchFramesForScene()` and mapped it as `cameraDirectionId` in the return     
     object
          ```typescript
          // In SELECT:
          start_continuity,
          ai_start_continuity,
          continuity_frame_prompt,
          camera_direction_id   // NEW

          // In return mapping:
          continuityFramePrompt: shot.continuity_frame_prompt || null,
          cameraDirectionId: shot.camera_direction_id || null,  // NEW
          startFrame: frameEntry.start,
          ```

        - **`src/types/scene.ts`** (MODIFIED)
          - Extended `ShotWithFrames` interface with `cameraDirectionId`
          ```typescript
          export interface ShotWithFrames {
            // ... existing fields ...
            // Camera direction (Phase G — established views)
            cameraDirectionId?: string | null;
            startFrame: Frame | null;
            endFrame: Frame | null;
          }
          ```

        - **`backend/src/routes/projectAssets.ts`** (MODIFIED — 2 new endpoints +   
     PUT update)
          - **Phase G endpoint**: `POST
     /:projectId/assets/:assetId/location-views/:viewId/establish-from-frame`       
            - Takes `{ frameImageUrl, shotId, sceneId }` in body
            - Fetches shot for camera metadata + scene for provenance labeling      
            - Updates location_view: `image_key_url = frameImageUrl`, `source =     
     'established'`, `established_from_scene = "Scene X, Shot Y"`,
     `established_from_shot_id = shotId`
            - Inherits `camera_distance` and `camera_height` from the shot
          - **Phase H endpoint**: `POST
     /:projectId/assets/:assetId/location-views/:viewId/generate-image`
            - Finds style reference: establishing view > primary direction > master 
     asset image
            - Returns 400 if no reference available ("Upload or generate at least   
     one reference image first")
            - Builds prompt from view description + alias + camera metadata + asset 
     description
            - Uses `jobType: 'scene_asset'` (NOT `master_asset`) to avoid
     auto-updating `project_assets.image_key_url`
            - Passes explicit `width: 1024, height: 576` for 16:9 cinematic
            - Style reference passed as `referenceImageUrls: [{ url, role: 'style'  
     }]`
          - **PUT endpoint updated**: Added `image_key_url` to accepted update      
     fields
            ```typescript
            const { alias, description, camera_distance, camera_height, is_primary, 
     image_key_url } = req.body;
            // ...
            if (image_key_url !== undefined) updates.image_key_url = image_key_url; 
            ```

        - **`src/lib/services/projectAssetService.ts`** (MODIFIED — 2 new methods + 
     1 updated)
          - **`establishViewFromFrame()`**: Phase G — POST to establish-from-frame  
     endpoint
          - **`generateLocationViewImage()`**: Phase H — POST to generate-image,    
     polls via `pollImageJob()`, then PUTs `image_key_url` to the location view on  
     completion
            ```typescript
            async generateLocationViewImage(projectId, assetId, viewId):
     Promise<ImageGenerationJobResponse> {
              // POST to generate-image endpoint
              // Poll for completion
              // If completed, PUT image_key_url to location view
            }
            ```
          - **`updateLocationView()`**: Added `image_key_url?: string` to accepted  
     updates type

        - **`src/components/pipeline/EstablishViewPrompt.tsx`** (CREATED — Phase G  
     UI)
          - Props: `projectId, sceneId, shotId, frameImageUrl, frameStatus,
     directionView, locationAssetId, locationName, onEstablished`
          - All hooks (useState, useMutation) called before conditional return      
          - Two render modes:
            - **Gap fill** (no existing image): Emerald banner with "This fills a   
     coverage gap!" + "Lock as Established View" button
            - **Replace** (has existing image): Subtle text link "Lock as
     established view for {directionLabel}" that opens AlertDialog with side-by-side      image comparison
          - `useMutation` calls `projectAssetService.establishViewFromFrame()`,     
     invalidates coverage queries on success

        - **`src/components/pipeline/Stage10FrameGeneration.tsx`** (MODIFIED — Phase      G integration)
          - New imports: `EstablishViewPrompt`, `sceneAssetService`,
     `projectAssetService`, `LocationView`
          - Added 4 new queries/memos after progress calculation:
            1. `directionIds` — unique camera_direction_ids from shots
            2. `sceneAssetsData` — fetches scene assets (queryKey:
     `scene-assets-stage10`)
            3. `locationViewsMap` — Map<viewId, LocationView> for all location      
     assets (queryKey: `location-views-stage10`)
            4. `directionLookup` — Map<viewId, { view, assetId, locationName }>     
          - Renders `EstablishViewPrompt` after start frame FramePanel using IIFE:  
            ```typescript
            {(() => {
              const dirId = selectedShot.cameraDirectionId;
              const info = dirId ? directionLookup.get(dirId) : undefined;
              if (!info) return null;
              return (
                <EstablishViewPrompt
                  projectId={projectId}
                  sceneId={sceneId}
                  shotId={selectedShot.id}
                  frameImageUrl={selectedShot.startFrame?.imageUrl ?? null}
                  frameStatus={selectedShot.startFrame?.status ?? 'pending'}        
                  directionView={info.view}
                  locationAssetId={info.assetId}
                  locationName={info.locationName}
                />
              );
            })()}
            ```

        - **`src/components/pipeline/Stage8/LocationCoveragePanel.tsx`** (MODIFIED —      Phase H UI)
          - New imports: `Sparkles`, `Loader2`,
     `Tooltip/TooltipContent/TooltipTrigger`
          - New state: `generatingViewIds: Set<string>` — tracks which views are    
     currently generating
          - New callbacks:
            - `handleGenerateViewImage(assetId, viewId)` — generates single view,   
     toasts result, invalidates queries
            - `handleGenerateMissing(coverage)` — batch generates all missing       
     directions sequentially
          - DirectionRow enhanced with new props: `isGenerating`, `onGenerate`,     
     `hasStyleReference`
            - Shows loading spinner when generating
            - Per-direction Sparkles button (visible when no image + has style      
     reference + not generating)
          - "Generate N Missing Views" batch button added at bottom of each
     location's collapsible content
            - Only shown when there are missing directions AND at least one
     reference image exists
            - Disabled while any view is generating

        - **Files READ but not modified** (for understanding patterns):
          - `src/components/pipeline/FramePanel.tsx` — Frame
     display/carousel/actions (958 lines)
          - `src/components/pipeline/Stage10FrameGeneration.tsx` — Full Stage 10    
     architecture (1456 lines)
          - `backend/src/services/image-generation/ImageGenerationService.ts` — Job 
     creation, execution, storage patterns
          - `backend/src/services/image-generation/ImageProviderInterface.ts` —     
     ReferenceImage interface with role
          - `backend/src/routes/frames.ts` — Frame fetch endpoint structure
          - `src/lib/services/frameService.ts` — Frontend frame service patterns    
          - `._docs/ui-and-theme-rules.md` — Cinematic dark theme design system     

     4. Errors and Fixes:
        - **React hooks rules violation**: `useMutation` in
     `EstablishViewPrompt.tsx` was called after an early return (`if (!isFrameReady 
     || ...) return null`), violating React hooks rules.
          - Fix: Moved all hooks (useState, useMutation) BEFORE the conditional     
     return. Changed derived values (`hasExistingImage`, `directionLabel`) to use   
     optional chaining (`directionView?.alias`) since they're now computed before   
     the null check.
        - **`master_asset` job type would update wrong table**: Using `jobType:     
     'master_asset'` with `assetId` in the generate-image endpoint would trigger the      completion handler to update `project_assets.image_key_url` instead of        
     `location_views.image_key_url`.
          - Fix: Changed to `jobType: 'scene_asset'` without passing `assetId` or   
     `sceneId`, so no auto-update triggers. Frontend handles updating
     `location_views.image_key_url` via PUT after polling.
        - **PUT endpoint didn't accept `image_key_url`**: The location view PUT     
     endpoint only whitelisted `alias, description, camera_distance, camera_height, 
     is_primary`.
          - Fix: Added `image_key_url` to the destructured body and update object.  
        - **Backend ESLint config error**: Pre-existing `TypeError: Error while     
     loading rule '@typescript-eslint/no-unused-expressions'` — not related to      
     changes, ignored.
        - **Pre-existing test failures**: 7 tests in `image-generation.test.ts` —   
     require API keys, pre-existing.

     5. Problem Solving:
        - **Data flow for established views**: Identified that `ShotWithFrames`     
     didn't include `camera_direction_id`, requiring changes to both the backend    
     query and frontend type. The direction lookup in Stage 10 needed scene assets →      location assets → location views → direction map, requiring 3 cascading       
     queries.
        - **Image storage isolation**: For Phase H batch generation, needed to avoid      the `master_asset` completion handler that would overwrite the main asset     
     image. Solved by using `scene_asset` job type without asset/scene IDs, letting 
     the image go to `misc/` storage path, and updating
     `location_views.image_key_url` explicitly after polling.
        - **Style reference cascade**: The generate endpoint implements a 3-tier    
     fallback for style reference: establishing view → primary direction → master   
     asset image, matching the spec's recommendation.

     6. All User Messages:
      

     7. Pending Tasks:
        - No explicit pending tasks. Phases G & H implementation is complete.       

     8. Current Work:
        Phases G and H are fully implemented. All files have been modified/created, 
     lint passes (only pre-existing issues), TypeScript compiles clean, all 242     
     frontend tests and 272 backend tests pass (with 7 pre-existing failures in     
     image-generation.test.ts). Memory has been updated. A full summary of changes  
     was provided to the user.

        Modified files:
        - `backend/src/routes/projectAssets.ts` (+257 lines)
        - `backend/src/services/frameGenerationService.ts` (+4 lines)
        - `src/components/pipeline/Stage10FrameGeneration.tsx` (+75 lines)
        - `src/components/pipeline/Stage8/LocationCoveragePanel.tsx` (+141 lines)   
        - `src/lib/services/projectAssetService.ts` (+102 lines)
        - `src/types/scene.ts` (+2 lines)

        Created files:
        - `src/components/pipeline/EstablishViewPrompt.tsx` (new)

     9. Optional Next Step:
        The implementation of Phases G & H is complete. All 8 phases (A-H) of the   
     location system enhancement are now done. No explicit next step was requested. 
     The user may want to:
        - Test the implementation manually via the UI
        - Write tests for the new endpoints and components
        - Commit the changes

     If you need specific details from before compaction (like exact code snippets, 
     error messages, or content you generated), read the full transcript at:        
     C:\Users\Daniel Lopez\.claude\projects\C--Users-Daniel-Lopez-Desktop-Aiuteur-wi     zardirector\0ec60e63-b148-49f8-8c90-9061d1abcf99.jsonl

