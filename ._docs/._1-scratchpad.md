*Entered Already*
Okay, in the previous task we added a 'Use as reference' toggle on regens within the upload modal in stage 5

Now I want you to find where else we should add this toogle functionality, as I think it's quite helpful feature. I think for sure with the stage ten frame generation we shoul dhave the option to regen with a previously uploaded image, but where else should we add it, and output a recommendation (NOTE: this toggle should be defaulted to off for every other instance except the image upload modal in stage 5)

--------------------------------
*More To-dos*

Probably consider this later:   4. Stage 8 Location Coverage Panel — per-direction generate (LocationCoveragePanel.tsx)
  - Where: The per-direction Sparkles button and "Generate N Missing Views" batch button.
  - Why: Currently the backend picks the establishing/primary view as a style reference automatically. A toggle would let users  
  explicitly choose which existing view image to use as reference, or opt out entirely.
  - Default: OFF (backend already handles its own reference logic, so the toggle would be an override).
  - Note: Lower priority because the backend already does something reasonable here.


To be clear, this toogle should only appear for when an image already exists in the component at hand, like for all applications; like the toggle should be invisible or unclickable if present in the view WHEN no image has been  uploaded/ generated. 


(move to also add the ability to drag/grab and drop frames into other frames, where that pulled frame can be used just as is (which i suspect will be rare) and more importantly, this view can be used as a starting point which can then be edited via regen & inpaint functionality & "edit image" funcitonality (the same "edit image functionality that exists with the stage 5 image upload modal "edit image" button) so there needs to be those buttons added as functionality ("edit image") and regen using prompt & the toggle to use the currently selected image for the frame panel..  where the frame prompt basically serves the same functionalty as the edit image text entered by user, like it's the key prompt for generation (similar functionality that the "edit image" does, but it sends the full description instead of a often brief "edit it like this - Edit image description in the "Regeneration box ))
